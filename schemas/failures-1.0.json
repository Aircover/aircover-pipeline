{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aircover.ai/schemas/aircover-pipeline/failures-1.0.json",
  "title": "Aircover Pipeline — failures.json (v1.0)",
  "description": "Per-meeting failure list. Only written when one or more rows failed; absent on fully-successful runs.",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["meeting_id"],
    "properties": {
      "meeting_id": {
        "type": "string",
        "description": "Aircover meeting identifier that failed processing."
      },
      "deal_id": {
        "type": "string",
        "description": "Mirrored from meetings.json. May be empty."
      },
      "date": {
        "type": "string",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
        "description": "Mirrored from meetings.json."
      }
    }
  }
}
